dependencies:
    override:
        - make install VIRTUALENV=virtualenv
    cache_directories:
        - eggs
test:
    override:
        - make test
    post:
        - cp output/* $CIRCLE_ARTIFACTS

