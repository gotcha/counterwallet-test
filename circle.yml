dependencies:
    override:
        - make install VIRTUALENV=virtualenv ROBOT_OUTPUT=$CIRCLE_TEST_REPORTS
    cache_directories:
        - eggs
test:
    override:
        - make test
    post:
        - cp $CIRCLE_TEST_REPORTS/* $CIRCLE_ARTIFACTS

